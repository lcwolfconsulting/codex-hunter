apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: codex-frontend-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: codex-frontend-service
            port:
              number: 80
